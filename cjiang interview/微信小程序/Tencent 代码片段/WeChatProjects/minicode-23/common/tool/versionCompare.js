/**
 * 版本号比较，判断 version 是否比 versionBase 基础版本高
 * @param version
 * @param versionBase
 * @returns {boolean}
 */

export const versionCompare = (version, versionBase) => {
  
};
/**
 * 判断滑块在支付宝上面是否能够使用
 * 内核版本号最低要求 2.7.0
 * ios手机版本最低要求 14.0.0
 * 支付宝最低版本10.2.0
 * 同时必须是ios手机
 */

export const versionSlide = () => {

  return true;
};
