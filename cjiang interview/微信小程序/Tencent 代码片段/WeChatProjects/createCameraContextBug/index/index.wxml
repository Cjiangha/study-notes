<view class="page-body">
  <view class="page-body-wrapper">
    <camera device-position="back" flash="off" binderror="error" style="width: 100vw; height: 100vh;">
      <view>{{ test }}</view>
      <view class="btn-area">
      <button type="primary" bindtap="startTimer" wx:if="{{btnShow}}" >开始录像</button>
      <button bindtap="tingzhi">停止录像</button>
    </view>
    <!-- <view class="btn-area">
      <button type="primary" bindtap="stopRecord">结束录像</button>
    </view> -->
    <view>视频个数：{{ recordArr.length }}</view>
    <view>秒数：{{ totalTime }}s/120s</view>
    <view>{{ src }}</view>
    <view>{{ videoSrc }}</view>
    </camera>
    <!-- <view class="btn-area">
      <button type="primary" bindtap="takePhoto">拍照</button>
    </view>
    <view class="btn-area">
      <button type="primary" bindtap="startRecord">开始录像</button>
    </view>
    <view class="btn-area">
      <button type="primary" bindtap="stopRecord">结束录像</button>
    </view>
    <view class="preview-tips">预览</view>
    <image wx:if="{{src}}" mode="widthFix" src="{{src}}"></image>
    <video wx:if="{{videoSrc}}" class="video" src="{{videoSrc}}"></video> -->
  </view>
</view>